import { ConfigSettings } from "./config.settings";
import { MediaSettings } from "./media.settings";
import { ColorSettings, StyleSettings } from "./style.settings";

export { ColorSettings, MediaSettings, StyleSettings, ConfigSettings };
